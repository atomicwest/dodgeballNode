function avatar(color, radius, x, y, vx, vy){
    this.color = color;
    this.radius = radius,
    this.x = x;
    this.y = y;
    this.vx = vx;
    this.vy = vy;
    this.fillArc
}

module.exports(avatar);